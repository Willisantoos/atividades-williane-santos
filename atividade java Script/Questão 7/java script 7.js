let numeros = [1,2,3,4,5];
        let numerosduplicados = []
        numeros.forEach(elemento => {
            numeros.push(elemento * 2)
        });
        console.log(numerosduplicados.toString() )